namespace AutoLot.Domain.Enums;

/// <summary>
/// Тип кузова. Це замкнений перелік, тому він живе в коді як enum, а не в
/// таблиці: нові кузови з'являються раз на десятиліття, зате компілятор
/// стежить, щоб ніхто не вписав неіснуючий тип. Назви для показу лежать
/// у таблиці перекладів — див. EnumTranslation.
/// </summary>
public enum BodyType
{
    Sedan = 0,
    Hatchback = 1,
    Universal = 2,
    Coupe = 3,
    Crossover = 4,
    Minivan = 5,
    Pickup = 6,
    Convertible = 7,
    Liftback = 8,
    Van = 9,
    Limousine = 10,

    // Нові значення дописуємо в кінець із новими числами. Перенумерувати
    // наявні не можна: у базі зберігається саме назва значення, а порядок
    // показу задає файл перекладів, не ці числа.
    Microvan = 11,
    Minibus = 12,
    Roadster = 13,
}
